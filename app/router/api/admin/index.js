/** import express module */
const express = require('express');
/** create express Router instance */
const router = express.Router();

/** todo@ create and define api admin routers */

module.exports = router;